<div class="row" style="margin-top: 10px;">
    <div class="col s12 l12">
        <div class="center">
            <h1>Welcome to <?= $town ?></h1>
        </div>
    </div>
</div>
<div class="row">
    <div class="col s12 l12">
        <?php if (count($places) == 0) : ?>
            <div class="center">
                <img src="/images/no_records.png" class="responsive-img" />
                <h6 class="center">Oops! It seems there are no places yet declared in this town. Try again later or check out this <a href="/explore">site</a></h6>
            </div>
        <?php else : ?>
            <ul class="collection">
                <?php foreach ($places as $place) : ?>
                    <li class="collection-item" style="cursor: pointer;" onclick="window.location='/place/<?= $place['id'] ?>'">
                        <h5>
                            <?= $place['name'] ?>
                            <?php if ($place['arEnabled'] == 1) : ?>
                                <span class="new badge blue" data-badge-caption="AR Available">&nbsp;</span>
                            <?php endif; ?>
                        </h5>
                        <p class="hide-on-small-only"><?= $place['description'] ?></p>
                    </li>
                <?php endforeach; ?>
            </ul>
        <?php endif; ?>
    </div>
</div>